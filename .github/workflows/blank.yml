          
          
name: Deploy SNHU Release
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Release SNHU
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Stage
      uses: actions/github-script@v5
      with:
        script: |
          await github.rest.actions.createWorkflowDispatch({
            'Himanshu199430',
            'Backend',
            'workflows/blank.yml',
            'main',
          });
